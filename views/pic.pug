<!DOCTYPE html>
html(lang="en")
    head
        title!= options.title
    body
        - let photos = options.photos;
        div.page
            ol
                - for (let i = 0; i < photos.length; i++) 
                    li 
                        a(href=photos[i].url) #{photos[i].title}